import React from 'react';

const ScoreControls = (props) => null;
  // <div className="scoreControls">
  //     <div className="scoreControls__modifier--add" onClick={() => props.increment(props.side)}>+</div>
  //     <div className="scoreControls__modifier--subtract" onClick={() => props.decrement(props.side)}>-</div>
  //   </div>;

export default ScoreControls;
